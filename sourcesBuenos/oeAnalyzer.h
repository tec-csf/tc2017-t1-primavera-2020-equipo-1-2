#ifndef ANALYZE
#define ANALYZE

//Function prototypes
#include <vector>
#include <fstream>
#include <queue>

using namespace std;

void printArray(vector<int> a);

int findChar(string s, char x);

void analisisPrueba(queue<string> lineasComp);

#endif